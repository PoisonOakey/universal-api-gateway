apiVersion: v1
kind: Service
metadata:
  name: universal-api-gateway
  namespace: gateway-system
spec:
  type: NodePort
  selector:
    app: universal-api-gateway
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
      nodePort: 30000
